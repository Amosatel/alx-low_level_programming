#include "main.h"

/**
 * mul - mmultiplies two integers
 * @a: the first number to be multiplied
 * @b: number to be multiplied with first number
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}
