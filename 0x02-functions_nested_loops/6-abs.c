#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @x: integer whose value is computed
 * Return: absolute value
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
